#!/bin/bash

cd addons

for addon in *; do
  if [ -d $addon ] && [ -f "$addon/$addon.php" ]; then
    cd $addon && npm run build && cd -
  fi
done

cd ../custom-blocks

for block in *; do
	if [ -d $block ]; then
		cd $block && npm run build && cd -
	fi
done

